[project]
dependencies = [
  "qtpie",
  "qtpie_samples",
  "qtpie_test",
]
description = "Tests for QtPie"
name = "tests"
requires-python = ">=3.12"
version = "0.1.0"

[tool.uv.sources]
qtpie = {workspace = true}
qtpie_samples = {workspace = true}
qtpie_test = {workspace = true}
