[project]
dependencies = [
  "qtpie[test]",
  "qtpie_samples",
]
description = "Tests for QtPie"
name = "tests"
requires-python = ">=3.13"
version = "0.1.0"

[tool.uv.sources]
qtpie = {workspace = true}
qtpie_samples = {workspace = true}
