[project]
dependencies = [
    "assertpy>=1.1",
    "forc2",
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
    "pytest-sugar>=1.1.1",
    "qtpie[test]",
]
description = "Tests for Forc2"
name = "forc2-tests"
requires-python = ">=3.13"
version = "0.1.0"
